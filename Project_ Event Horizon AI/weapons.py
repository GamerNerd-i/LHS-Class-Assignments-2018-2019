#Event Horizon Weapon Data

sword = {"Color": R,
         "Type": P,
         "Eff": N}

lance = {"Color": B,
         "Type": P,
         "Eff": N}

axe = {"Color": G,
         "Type": P,
         "Eff": N}

bow = {"Color": N,
         "Type": P,
         "Eff": fly}

dagger = {"Color": N,
         "Type": P,
         "Eff": N}

heal = {"Color": N,
         "Type": M,
         "Eff": 1/2}

gun = {"Color": N,
         "Type": P,
         "Eff": N}

rmag = {"Color": R,
         "Type": M,
         "Eff": N}

bmag = {"Color": B,
         "Type": M,
         "Eff": N}

gmag = {"Color": R,
         "Type": M,
         "Eff": N}

advantage = {"color" = 1.2,
             "defensive" = 0.7,
             "eff" = 1.5}

disadvantage = {"color" = 0.8,
                "defensive" = 0.7,
                "eff" = 0.5}
